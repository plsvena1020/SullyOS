// Spike/Phase 1 stand-in types for optional native shells.
// These packages are NOT installed until the Android/Windows shell tasks land.
// When `@capacitor/core` / `@tauri-apps/api` are added to package.json,
// delete this file so the real package types take over.
declare module '@capacitor/core';
declare module '@tauri-apps/api/core';
declare module '@tauri-apps/api/event';
